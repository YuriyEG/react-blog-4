import React from 'react';

import styles from './signInButton.module.css';

const SignInButton = () => {
  return <div className={styles.signInButton}>Sign In</div>;
};

export default SignInButton;
